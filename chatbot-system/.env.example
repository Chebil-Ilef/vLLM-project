# Example environment variables for chatbot-system

# Neo4j connection (use bolt://neo4j:7687 when running via docker-compose)
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=test

## vLLM server configuration (vLLM is mandatory)
# URL for the vLLM OpenAI-compatible API. When using docker-compose the service is `vllm` and default URL is http://vllm:8000
VLLM_URL=http://vllm:8000

# API key configured for the vLLM server (set to the same value passed to the vllm container with --api-key)
VLLM_API_KEY=devkey

# Model identifier for the vLLM server to load (example shown in docker-compose)
VLLM_MODEL=mistralai/Mistral-7B-v0.1
